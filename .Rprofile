options(tab.width = 8)
options(width = 80)
.First 	<-	function()	{
			setwd("D:/R_data")
			dir_now	<-	getwd()
			cat("\n Welcome this day:",date(),
			"your current directory is",dir_now,"\n",sep=" ")
		}